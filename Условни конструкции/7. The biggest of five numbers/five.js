function solve(args)
{
    var a = +args[0],
        b = +args[1],
        c = +args[2],
        d = +args[3],
        e = +args[4];
    var biggest =Math.max(a, b, c, d, e);
    console.log(biggest);
}